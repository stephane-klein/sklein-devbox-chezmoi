#!/usr/bin/env bash
# nvim config hash: {{ $dir := joinPath .chezmoi.sourceDir "dot_config/nvim" }}{{ $cmd := printf "find %q -type f -print0 2>/dev/null | sort -z | xargs -0 sha256sum 2>/dev/null | sha256sum | cut -d' ' -f1" $dir }}{{ output "sh" "-c" $cmd }}
~/bin/install-or-update-neovim.sh
